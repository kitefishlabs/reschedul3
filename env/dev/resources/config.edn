{:dev true
 :port 3000
 ;; when :nrepl-port is set the application starts the nREPL server on load
 :nrepl-port 7000

 :database-url "mongodb://127.0.0.1/reschedul2-testdb"

 :permission-levels ["basic"          ; can create, view, edit, delete owned items; 
                     "organizer"      ; can assign, edit all (most) items, update public pages
                     "admin"]

 :reset-users true}
